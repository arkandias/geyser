table:
  name: phase
  schema: public
select_permissions:
  - role: assigner
    permission:
      columns:
        - current
        - value
      filter: { }
    comment: Les assigners peuvent voir toutes les phases.
  - role: teacher
    permission:
      columns:
        - current
        - value
      filter: { }
    comment: Les teachers peuvent voir toutes les phases.
