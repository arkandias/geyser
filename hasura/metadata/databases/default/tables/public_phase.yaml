table:
  name: phase
  schema: public
select_permissions:
  - role: commissioner
    permission:
      columns:
        - current
        - value
      filter: { }
    comment: Les commissioners peuvent voir toutes les phases.
  - role: teacher
    permission:
      columns:
        - current
        - value
      filter: { }
    comment: Les teachers peuvent voir toutes les phases.
