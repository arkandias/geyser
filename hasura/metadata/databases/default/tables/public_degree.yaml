table:
  name: degree
  schema: public
array_relationships:
  - name: programs
    using:
      foreign_key_constraint_on:
        column: degree_id
        table:
          name: program
          schema: public
select_permissions:
  - role: commissaire
    permission:
      columns:
        - id
        - name
        - name_short
        - visible
      filter:
        _exists:
          _table:
            name: phase
            schema: public
          _where:
            _and:
              - value:
                  _eq: commission
              - current:
                  _eq: true
    comment: En phase de commission, les commissaires peuvent voir tous les degree.
  - role: teacher
    permission:
      columns:
        - id
        - name
        - name_short
        - visible
      filter:
        _and:
          - _exists:
              _table:
                name: phase
                schema: public
              _where:
                _and:
                  - value:
                      _neq: fermeture
                  - current:
                      _eq: true
          - visible:
              _eq: true
    comment: Lorsque Geyser est ouvert, les teachers peuvent voir tous les degree visibles.
