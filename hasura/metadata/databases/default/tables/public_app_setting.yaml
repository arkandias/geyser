table:
  name: app_setting
  schema: public
select_permissions:
  - role: commissioner
    permission:
      columns:
        - key
        - label
        - value
      filter: { }
      limit: 1
    comment: "Can view any app setting (restricted to 1 row per query)"
  - role: teacher
    permission:
      columns:
        - key
        - label
        - value
      filter: { }
      limit: 1
    comment: "Can view any app setting (restricted to 1 row per query)"
