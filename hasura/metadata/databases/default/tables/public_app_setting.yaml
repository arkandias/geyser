table:
  name: app_setting
  schema: public
select_permissions:
  - role: commissioner
    permission:
      columns:
        - key
        - label
        - value
      filter: { }
      query_root_fields:
        - select_by_pk
      subscription_root_fields: [ ]
    comment: "Can view any app setting (selection by primary key only)"
  - role: teacher
    permission:
      columns:
        - key
        - label
        - value
      filter: { }
      query_root_fields:
        - select_by_pk
      subscription_root_fields: [ ]
    comment: "Can view any app setting (selection by primary key only)"
